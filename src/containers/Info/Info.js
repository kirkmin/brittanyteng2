import React, { Component } from 'react';

import styles from './Info.module.css';

const Info = props => {
  props.setMainBackground('white');

  return (
    <div>
      Info
    </div>
  );
};

export default Info;